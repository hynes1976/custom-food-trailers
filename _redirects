# Cloudflare Pages serves .html files at extensionless paths automatically
# (e.g. /our-trailers serves our-trailers.html), so no clean-URL rules are needed.
# Only keep redirects from OLD Webflow URLs to the new pages:
/about-us                          /about          301
/guides/how-to-choose-a-trailer    /buying-guide   301
